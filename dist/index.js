import * as motionbank from 'motionbank';
import * as node from 'node';
import * as shared from 'shared';

var XLib = {
  motionbank: motionbank,
  node: node,
  shared: shared
};

export default XLib;